<Window x:Class="LabAnalyzerConnector.Views.AnalyzerMappingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Test Code Mapping"
        Width="1100"
        Height="700"
        MinWidth="900"
        MinHeight="600"
        WindowStartupLocation="CenterOwner"
        Background="#F5F7FA">

    <Window.Resources>

        <!-- =============================== -->
        <!-- BUTTON STYLE -->
        <!-- =============================== -->

        <Style x:Key="PrimaryButton"
               TargetType="Button">

            <Setter Property="Height"
                    Value="36"/>

            <Setter Property="Padding"
                    Value="18,0"/>

            <Setter Property="Margin"
                    Value="0,0,8,0"/>

            <Setter Property="FontWeight"
                    Value="SemiBold"/>

            <Setter Property="Foreground"
                    Value="White"/>

            <Setter Property="Background"
                    Value="#2563EB"/>

            <Setter Property="BorderThickness"
                    Value="0"/>

            <Setter Property="Cursor"
                    Value="Hand"/>

        </Style>


        <Style x:Key="SecondaryButton"
               TargetType="Button">

            <Setter Property="Height"
                    Value="36"/>

            <Setter Property="Padding"
                    Value="18,0"/>

            <Setter Property="Margin"
                    Value="0,0,8,0"/>

            <Setter Property="FontWeight"
                    Value="SemiBold"/>

            <Setter Property="Foreground"
                    Value="#374151"/>

            <Setter Property="Background"
                    Value="#FFFFFF"/>

            <Setter Property="BorderBrush"
                    Value="#D1D5DB"/>

            <Setter Property="BorderThickness"
                    Value="1"/>

            <Setter Property="Cursor"
                    Value="Hand"/>

        </Style>


        <Style x:Key="DangerButton"
               TargetType="Button">

            <Setter Property="Height"
                    Value="36"/>

            <Setter Property="Padding"
                    Value="18,0"/>

            <Setter Property="Margin"
                    Value="0,0,8,0"/>

            <Setter Property="FontWeight"
                    Value="SemiBold"/>

            <Setter Property="Foreground"
                    Value="#DC2626"/>

            <Setter Property="Background"
                    Value="#FEF2F2"/>

            <Setter Property="BorderBrush"
                    Value="#FECACA"/>

            <Setter Property="BorderThickness"
                    Value="1"/>

            <Setter Property="Cursor"
                    Value="Hand"/>

        </Style>


        <!-- =============================== -->
        <!-- TEXTBOX STYLE -->
        <!-- =============================== -->

        <Style TargetType="TextBox">

            <Setter Property="Height"
                    Value="36"/>

            <Setter Property="Padding"
                    Value="10,6"/>

            <Setter Property="FontSize"
                    Value="14"/>

            <Setter Property="BorderBrush"
                    Value="#D1D5DB"/>

            <Setter Property="BorderThickness"
                    Value="1"/>

            <Setter Property="Background"
                    Value="White"/>

        </Style>


        <!-- =============================== -->
        <!-- DATAGRID -->
        <!-- =============================== -->

        <Style TargetType="DataGrid">

            <Setter Property="Background"
                    Value="White"/>

            <Setter Property="BorderBrush"
                    Value="#E5E7EB"/>

            <Setter Property="BorderThickness"
                    Value="1"/>

            <Setter Property="RowHeight"
                    Value="42"/>

            <Setter Property="FontSize"
                    Value="14"/>

            <Setter Property="GridLinesVisibility"
                    Value="Horizontal"/>

            <Setter Property="HorizontalGridLinesBrush"
                    Value="#E5E7EB"/>

            <Setter Property="VerticalGridLinesBrush"
                    Value="#FFFFFF"/>

            <Setter Property="SelectionMode"
                    Value="Single"/>

            <Setter Property="SelectionUnit"
                    Value="FullRow"/>

        </Style>


        <Style TargetType="DataGridColumnHeader">

            <Setter Property="Background"
                    Value="#F9FAFB"/>

            <Setter Property="Foreground"
                    Value="#374151"/>

            <Setter Property="FontWeight"
                    Value="SemiBold"/>

            <Setter Property="Padding"
                    Value="12,8"/>

            <Setter Property="BorderBrush"
                    Value="#E5E7EB"/>

            <Setter Property="BorderThickness"
                    Value="0,0,0,1"/>

        </Style>

    </Window.Resources>


    <!-- ===================================================== -->
    <!-- MAIN -->
    <!-- ===================================================== -->

    <Grid Margin="30">

        <Grid.RowDefinitions>

            <RowDefinition Height="Auto"/>

            <RowDefinition Height="Auto"/>

            <RowDefinition Height="Auto"/>

            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>


        <!-- ================================================= -->
        <!-- HEADER -->
        <!-- ================================================= -->

        <Grid Grid.Row="0"
              Margin="0,0,0,25">

            <StackPanel>

                <TextBlock
                    Text="Test Code Mapping"
                    FontSize="30"
                    FontWeight="Bold"
                    Foreground="#111827"/>

                <TextBlock
                    Text="Map analyzer test codes to standard LOINC codes"
                    FontSize="14"
                    Foreground="#6B7280"
                    Margin="0,6,0,0"/>

            </StackPanel>

        </Grid>


        <!-- ================================================= -->
        <!-- FORM CARD -->
        <!-- ================================================= -->

        <Border Grid.Row="1"
                Background="White"
                CornerRadius="10"
                Padding="22"
                BorderBrush="#E5E7EB"
                BorderThickness="1"
                Margin="0,0,0,18">

            <Grid>

                <Grid.ColumnDefinitions>

                    <ColumnDefinition Width="*"/>

                    <ColumnDefinition Width="*"/>

                    <ColumnDefinition Width="*"/>

                    <ColumnDefinition Width="Auto"/>

                </Grid.ColumnDefinitions>


                <!-- Analyzer Code -->

                <StackPanel Grid.Column="0"
                            Margin="0,0,15,0">

                    <TextBlock
                        Text="Analyzer Test Code"
                        FontWeight="SemiBold"
                        Foreground="#374151"/>

                    <TextBox
                        x:Name="AnalyzerTestCodeTextBox"
                        Margin="0,7,0,0"/>

                </StackPanel>


                <!-- LOINC -->

                <StackPanel Grid.Column="1"
                            Margin="0,0,15,0">

                    <TextBlock
                        Text="LOINC Code"
                        FontWeight="SemiBold"
                        Foreground="#374151"/>

                    <TextBox
                        x:Name="StandardTestCodeTextBox"
                        Margin="0,7,0,0"/>

                </StackPanel>


                <!-- Test Name -->

                <StackPanel Grid.Column="2"
                            Margin="0,0,15,0">

                    <TextBlock
                        Text="Standard Test Name"
                        FontWeight="SemiBold"
                        Foreground="#374151"/>

                    <TextBox
                        x:Name="StandardTestNameTextBox"
                        Margin="0,7,0,0"/>

                </StackPanel>


                <!-- Save -->

                <Button
                    Grid.Column="3"
                    x:Name="SaveButton"
                    Content="Save Mapping"
                    Width="135"
                    Margin="5,23,0,0"
                    Style="{StaticResource PrimaryButton}"
                    Click="SaveButton_Click"/>

            </Grid>

        </Border>


        <!-- ================================================= -->
        <!-- ACTION BAR -->
        <!-- ================================================= -->

        <Grid Grid.Row="2"
              Margin="0,0,0,15">

            <Grid.ColumnDefinitions>

                <ColumnDefinition Width="*"/>

                <ColumnDefinition Width="Auto"/>

            </Grid.ColumnDefinitions>


            <!-- Left -->

            <StackPanel
                Grid.Column="0"
                Orientation="Horizontal">

                <Button
                    x:Name="EditButton"
                    Content="Edit Selected"
                    Style="{StaticResource SecondaryButton}"
                    Click="EditButton_Click"/>

                <Button
                    x:Name="DeleteButton"
                    Content="Delete Selected"
                    Style="{StaticResource DangerButton}"
                    Click="DeleteButton_Click"/>

                <Button
                    x:Name="ClearButton"
                    Content="Clear"
                    Style="{StaticResource SecondaryButton}"
                    Click="ClearButton_Click"/>

            </StackPanel>


            <!-- Right -->

            <TextBlock
                Grid.Column="1"
                Text="Select a mapping to edit or delete"
                VerticalAlignment="Center"
                Foreground="#9CA3AF"
                FontSize="13"/>

        </Grid>


        <!-- ================================================= -->
        <!-- TABLE CARD -->
        <!-- ================================================= -->

        <Border Grid.Row="3"
                Background="White"
                CornerRadius="10"
                BorderBrush="#E5E7EB"
                BorderThickness="1">

            <Grid>

                <Grid.RowDefinitions>

                    <RowDefinition Height="Auto"/>

                    <RowDefinition Height="*"/>

                </Grid.RowDefinitions>


                <!-- Table Header -->

                <Border
                    Grid.Row="0"
                    Background="#F9FAFB"
                    Padding="18,14"
                    BorderBrush="#E5E7EB"
                    BorderThickness="0,0,0,1">

                    <Grid>

                        <TextBlock
                            Text="Configured Test Mappings"
                            FontSize="16"
                            FontWeight="SemiBold"
                            Foreground="#111827"/>

                    </Grid>

                </Border>


                <!-- Table -->

                <DataGrid
                    Grid.Row="1"
                    x:Name="MappingsGrid"
                    AutoGenerateColumns="False"
                    IsReadOnly="True"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False">

                    <DataGrid.Columns>

                        <DataGridTextColumn
                            Header="Analyzer Code"
                            Binding="{Binding AnalyzerTestCode}"
                            Width="2*"/>

                        <DataGridTextColumn
                            Header="LOINC Code"
                            Binding="{Binding StandardTestCode}"
                            Width="2*"/>

                        <DataGridTextColumn
                            Header="Standard Test"
                            Binding="{Binding StandardTestName}"
                            Width="3*"/>

                        <DataGridCheckBoxColumn
                            Header="Active"
                            Binding="{Binding IsActive}"
                            Width="90"/>

                    </DataGrid.Columns>

                </DataGrid>

            </Grid>

        </Border>

    </Grid>

</Window>